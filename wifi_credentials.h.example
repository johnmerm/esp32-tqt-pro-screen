#pragma once

// Copy this file to wifi_credentials.h and fill in your network credentials.
// wifi_credentials.h is git-ignored so secrets never leave your machine.

struct WifiCred { const char* ssid; const char* password; };

static const WifiCred WIFI_KNOWN[] = {
    { "YourNetworkSSID",   "YourPassword"   },
    // { "AnotherSSID",    "AnotherPassword" },  // add more as needed
};